db_user = {
    "admin": {
        "id": 0,
        "username": "admin",
        "password": "admin",
        "full_name": "admin full",
        "npm": None,
    }
}

db_client = {
    "core-web": {
        "client_secret": "ssst, it's a secret",
    }
}

db_access_token = {}

db_refresh_token = {}
